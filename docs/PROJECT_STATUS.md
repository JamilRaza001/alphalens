| Spec | Title | Status | Commit | Notes |
|------|-------|--------|--------|-------|
| S1 | Repo Scaffold | DONE | `11b6a38` | pyproject, ruff, pre-commit, .env.example, dirs |
| S2 | DB Schema + Seed | DONE | `31c7d16` | Neon pgvector schema, 10-company seed, CLAUDE.md |
| S3 | Cloud Resources | DONE | `200d957` | ECR repo + 3-image lifecycle, R2 multipart cleanup (7d), log retention helper, verify_s3.sh 8/8 |
| S4 | Settings (Config) | DONE | `6fab367` | pydantic-settings v2, L17 jina_dimensions lock, L11 OIDC-in-Lambda guard, lru_cache singleton |
| S5 | EDGAR Client | DONE | `a3adc27` | async SEC client, R2 cache-through, token-bucket rate limiter (10 req/s), tenacity retry (429/5xx), SEC_EDGAR_USER_AGENT validator |
| S6 | Section Detector | DONE | `313eaf7` | hybrid text-pattern detection (not DOM), 10-K flat + 10-Q Part I/II disambig, table strip+count, TOC guard, O7 unstructured fallback; 15 unit + 1 real-10-K integration test |
| S7 | Chunker | DONE | `662a78a` | token/section/sentence-aware chunking, ~400 tok/50 overlap, spaCy splitter + nomic counter (DI), oversized-sentence + unstructured fallback, per-section index; 21 unit + 1 real-models integration test |
| S8 | Embedding Client | DONE | `bb038ff` | Jina v3 primary (truncate_dim=768) + nomic-embed-text-v1.5 fallback (native 768d), async w/ lazy nomic load + to_thread, quota auto-flip (8M/10M), 402 whole-call re-embed, 768d asserts, RRF-ready; einops dep added; 20 unit + 1 real-models integration test
| S9 | Upsert Pipeline | DONE | `caefd23` | EmbeddedChunk assembly (count + 768d check, global re-index, per-section idx in metadata, model stamp) + idempotent batched upsert (ON CONFLICT filing_id,chunk_index) + mixed-model + corpus guards; pgvector codec + JSONB; 15 unit + 1 real-Neon integration |
| S10 | Filing State Machine | DONE | `720a7bd` | FilingStatus/JobStatus/IngestionStep StrEnums; MAX_ATTEMPTS=3; COUNT-based retry (no retry_count col); app-side exponential backoff 2/4/8s; claim_retryable_filings two-query + Python filter; caller-owned transactions; 12 unit + 1 real-Neon integration test |
| S11 | ETL Runner | DONE | `c37c8d3` | discover + run orchestrator per Spec 11; drives the 151/151 backfill; runner.py + m01 R2-key migration + 614-line unit suite. S22 hardening (atomic attempt writes, stale-running reaper, meaningful exit codes) added later in `9614651` |
| S12 | Agent State | DONE | `ca832cc` | AgentState TypedDict + QueryPlan/RetrievedChunk/ScoredChunk/Citation/TimeRange (Pydantic v2); D1 confidence Literal["low","high"], D2 single-source query_plan, D3 confidence_reason, D4 RetrievedChunk (agent-side, distinct from ETL Chunk); L5 reducer/L6/L7/L19; live-DB verified (UUIDs, single chunks→filings join, section nullable); mypy --strict clean; spec c0a0f50 |
| S13 | Agent Nodes | DONE | `e9dc3a2` | 5 nodes (Plan/Retrieve-stub/Rerank/Evaluate/Synthesize) as standalone `(state, runtime)→dict` units + prompts.py; Choice B DI via LangGraph Runtime static context; D2 Groq gpt-oss-120b, D3 strict json_schema extraction (Plan→QueryPlan, Evaluate→EvalVerdict), D4 two-signal evaluate w/ coverage precedence (LLM skipped on gaps); Choice A retrieve_node NotImplementedError stub (body→S15); input-rail validate_tickers drop-and-note→unavailable_tickers; stream_synthesis seam (S14 wraps); piggybacks: EvalVerdict (reasoning-first) + unavailable_tickers key + rerank_top_n=5; 14 unit tests, mypy --strict clean; spec docs/specs/S13_agent_nodes.md |
| S14 | Circuit Breaker | DONE | `d570371` | `SynthesisCircuitBreaker` (consecutive-failure, in-memory per-container) guarding the Groq synthesis stream; D1 `is_hard_failure` (5xx/timeout/connection→count, 429+non-429 4xx→excluded+re-raised; live-verified langchain-groq surfaces native `groq.*` exceptions); D2 threshold=3 (env `BREAKER_FAILURE_THRESHOLD`), D3 30s monotonic reset (env `BREAKER_RESET_TIMEOUT_SECONDS`), D4 single HALF_OPEN probe w/ timer restart-from-zero on probe fail; L8; `BreakerState` StrEnum CLOSED/OPEN/HALF_OPEN; `except Exception` (CancelledError propagates); pre-first-token hard fail→degraded fallback, mid-stream→propagate; piggybacks: `AgentContext.breaker` + `synthesize_node` routes S13 `stream_synthesis` seam through `breaker.stream()`, `degraded_stream` (AgentState-agnostic) in nodes.py, 2 Settings fields; 18 unit tests (fakes only), mypy --strict clean; spec docs/specs/S14_circuit_breaker.md |
| S15 | Retrieve Node | DONE | `24f1818` | Real `retrieve_node` per-cell fan-out hybrid retrieval (replaces S13 stub); D3 cell = `(ticker × year × sub_question)` cartesian; per-cell parameterized SQL — HNSW cosine (`$1::vector`) + lexical `ts_rank_cd` (NOT BM25, L1) fused via RRF over a `FULL OUTER JOIN` (single-retriever hits survive), single `chunks→filings` join for in-shot enrichment, `c.embedding IS NOT NULL` guard; D1 config knobs `retrieval_k_vector/k_lexical=20`, `rrf_c=60`, `n_per_cell=10` (env-overridable); D2 `rerank_top_n` default 5→8; D4 `AgentContext.embedder` (cold-start jina-v3); INVARIANT jina-v3 query embeddings enforced — nomic fallback = loud `RuntimeError`, no DB touched; embed once per UNIQUE sub_question, `asyncio.gather` fan-out bounded by pool (max 5); helpers `plan_to_cells`/`merge_dedup` (chunk_id dedup, order-stable)/`rrf_fuse` (pure Python RRF reference mirroring the SQL, kept-in-sync); `_decode_metadata` (jsonb returns str → `json.loads`, tolerates dict/NULL); reuses S9 `register_pgvector` codec (S16 must build agent pool with `init=register_pgvector` — pinned in spec `f7a7fd0`); 11 new unit tests (fakes only, AC1/AC3–AC9), removed obsolete stub test; mypy --strict clean; spec docs/specs/S15_retrieve_node.md |
| S16 | Graph Wiring | DONE | `482f37b` | Linear single-pass `StateGraph(AgentState, context_schema=AgentContext)` (L19) compiled WITHOUT a checkpointer — `START→plan→retrieve→rerank→evaluate→synthesize→END`, no conditional edges (D3b linear lock); D1a two-module split: `graph.py` = pure topology (import-safe: no DB, no await, no model load) + `context.py` = `build_context()` cold-start seam returning `(AgentContext, Pool)` so the CALLER owns pool teardown (G7, mirrors etl/runner); **D1b RE-LOCKED post-G1: agent pool on the POOLED endpoint** — `agent_db_url` defaults to `neon_database_url` via validator (env `AGENT_DB_URL` overrides for a scale-flip), `min_size=1`/`max_size=5`, prepared statements ON (no `statement_cache_size=0`), `init=register_pgvector` reusing the S9 codec (G1 verified the runner runs pooled live — spec text said direct, code mirrors the runner); D2 streaming = `ainvoke()` → drain `final_state["answer_stream"]` in the caller (no `astream`/`stream_mode`; breaker gate fires lazily at drain, G3); D3a `load_ticker_universe` — one `SELECT ticker, name FROM companies` → `allowed_tickers` frozenset (hard rail) + `ticker_roster` (ticker→name) baked into the byte-stable Plan system prompt for grounded word→ticker resolution; D-temp: Plan/Evaluate re-pin `temperature=0` invariantly via `model_copy` (structured decoding lock), shared `llm` carries `groq_temperature` = Synthesize-only; D4 harness `scripts/run_query.py` (first real live run: `$1::vector` binding + SQL RRF + `chunks→filings` join + breaker drain all fire outside fake pools); piggybacks: `AgentContext` +`ticker_roster` +`corpus_min_year`/`corpus_max_year` (7→9 fields), `build_plan_system_prompt(allowed_tickers, ticker_roster)`, 3 Settings fields (`agent_db_url`/pool sizes). **Plan year-rail (post-live fix, same commit):** live run #1 returned a false "no coverage" — root-caused to Plan emitting `time_range.years=[20232024]` (concatenated "2023 vs 2024") at temp-0 under `strict=True` despite an explicit negative example; fixed with soft-guide (prompt) + hard-gate (`validate_years` vs `corpus_min_year=2021`/`corpus_max_year=2026`, drop-and-note → new `unavailable_years` state key, mirroring `unavailable_tickers`) + deterministic `split_concatenated_years` repair running UPSTREAM of the gate (multiple-of-4 digit-length → split, order-preserving dedup, WARNING log); Synthesize honesty-rail surfaces `unavailable_years` clause-parallel to `unavailable_tickers`; AC14 live test = `@pytest.mark.live` SCAFFOLD only (`pytest.skip`, structural assertions still TODO); 191 passed/7 skipped/1 deselected, mypy --strict clean; spec docs/specs/S16_graph_wiring.md |
| S17 | Selection Floor | DONE | `d934222` | per-pair floor + capacity budget replacing global top-N; `floor_per_pair=2`, `max_context_chunks=8` (cap sized to Groq free-tier TPM); `rerank_top_n` REMOVED; three branches by `n_pairs` (distinct `(ticker, period_year)` among retrieval survivors): ≤4 normal (full floor), 5–8 graduated (all pairs kept, depth cut to 1), ≥9 hard overflow (8 strongest kept, rest → `dropped_for_capacity`) — only branch (c) populates the drop list; spec doc's illustrative cap of 20 is STALE vs live 8 |
| S18 | FastAPI + SSE Local App | DONE | `d2109be` | D1 wrap-only over the S16 graph — zero node, graph, prompt or state changes; the agent is imported, never touched; D2 cold start via FastAPI `lifespan`: `build_context()` runs ONCE at startup, `(ctx, pool, graph)` stashed on `app.state` (`agent_context`/`pool`/`graph`), pool closed exactly once on teardown in a `finally` (AC2/AC3, mirrors `run_query.py:81`); D3 streaming inherits S16 D2 verbatim — `ainvoke()` then drain `final_state["answer_stream"]` in the caller, `astream`/`stream_mode` still rejected, so S14's breaker gate keeps firing lazily at drain; D5 `POST /query`, NOT `GET` — the browser's native `EventSource` is GET-only and cannot carry a JSON body, **so S19 must consume the stream with `fetch` + `ReadableStream`, not `EventSource`**; D6 `sse-starlette`'s `EventSourceResponse` (SSE framing, 15s `ping` keepalive, `_listen_for_disconnect`) over a hand-rolled `StreamingResponse`; **Q8 — every event payload is JSON-encoded, tokens included** (`{"text": …}`): a raw `data:` line is NOT newline-safe (SSE splits on `\n`), and `degraded_stream` plus `LOW_CONFIDENCE_CAVEAT` both yield newline-terminated tokens, so this is a live case and not a hypothetical; wire contract `meta` → `token`* → `citations` → `done` (AC4, AC15 — `citations` is ONE event carrying an array, emitted even when empty), with `error` as a MUTUALLY-EXCLUSIVE terminal event (never `done` after `error`) carrying `phase` = `"graph"` (including `answer_stream is None`) or `"stream"`; `breaker_open: bool` carried on BOTH terminal events (`DoneEvent` and `ErrorEvent`) so the degraded signal survives an aborted run; D8 CORS via an explicit `create_app(cors_origins: list[str] | None = None)` parameter defaulting to `DEFAULT_CORS_ORIGINS = ["http://localhost:3000", "http://127.0.0.1:3000"]`, never `["*"]` — **no new `Settings` field**, because reading config inside `create_app()` would call `get_settings()` and break AC1's no-`.env` import-safety; D7 no auth, no OIDC, no rate limiting, bound to `127.0.0.1` only (Vercel OIDC is Lambda-only, L11); `try`/`finally await stream.aclose()` around the drain so a client disconnect reaps the breaker-wrapped generator instead of leaking it (AC10); **spec amendments** — AC8 footer parity was amended (13 Aug 2026) to an INTERSECTION check because the original "every `MetaEvent` key appears in the footer" was false: the footer carries `latency` (absent from `MetaEvent`) and omits `request_id`; AC15 was APPENDED rather than inserted, deliberately, because AC8 and AC9 are referenced by number elsewhere in the spec; **live verification (16 Aug 2026), rubric frozen before the run** — G1 `meta` first PASS; G3 progressive token delivery PASS; AC4 event order PASS; Q8 JSON encoding PASS (the answer came back as a markdown table, so literal newlines rode inside `token` payloads — the exact case the encoding was locked for, exercised by accident); AC10 mid-stream disconnect PASS (no traceback, `/health` still 200 after); verification landed on the SECOND attempt — an earlier run stalled, and that stall was traced to the S15/S16 concurrent DB path, NOT to S18; **known limitations carried forward** — AC5 confirmed live: `meta` is emitted AFTER `ainvoke()` returns, not at Plan time, so the honesty-rail fields do arrive before the answer but LATE (~10s of dead air), and plan-time emission would break S16 D2, so it is v2; the CLOSED-boundary gap is DOCUMENTED, NOT FIXED, and deferred to v2 — `breaker_open` reads `False` when a CLOSED breaker hard-fails at drain and serves `degraded_stream` while still below threshold; adds `cast(AgentState, await graph.ainvoke(...))` at `app.py:169` (langgraph types the return as `dict[str, Any] | Any`, which `mypy --strict` rejects) — housekeeping item; spec FROZEN at `0829ac0` after 3 amendments; spec docs/specs/S18_fastapi_sse_local.md |
| Doc | Schema Reconcile — queries as 5th table | DONE | `7d065cb` | queries DDL added to §6.1 + S2 spec + Phase1 guide; financial_facts/entities correctly marked v2-deferred everywhere; decision #9 locked in schema_reconcile_to_live.md |
| Chore | pydantic.mypy plugin | DONE | `b0ac2e6` | strict kwarg checking on Pydantic models; dropped now-redundant type-ignore in config.py |
| C1 | Capacity/Coverage Split | DONE | `f94652d` | Fixes conflation where S17's capacity trim (`dropped_for_capacity`) was read by `evaluate_node` as absent evidence, forcing `confidence=low`/`reason=coverage` — two distinct causes (no-evidence vs budget-trim) collapsed into one signal. Set-membership partition performed IN Evaluate; `compute_coverage_gaps` left untouched as a pure `needed - present` primitive; new write-only v1 state key `capacity_drops`. Recon found the Synthesize disclosure plumbing already wired (prompts.py), so prose distinguishes both causes with no extra work. 4 new tests (AC1 = S4 regression guard); gates green, mypy --strict clean; spec `efcbbaf` (docs/specs/S_C1_capacity_coverage_split.md) |
| Chore | run_query footer surfacing | DONE | `ee404e1` | Harness footer printed neither `coverage_gaps` nor `capacity_drops`, making the C1 split invisible in live runs. +2 lines, scripts-only. Footer as of `ee404e1` read: `confidence`, `reason`, `coverage_gaps`, `capacity_drops`, `unavailable_tickers`, `unavailable_years`, latency. **Since `37e4b41` it also prints `unavailable_companies` and `plan_tickers`** (both inserted before `latency`), so the current footer is: `confidence`, `reason`, `coverage_gaps`, `capacity_drops`, `unavailable_tickers`, `unavailable_years`, `unavailable_companies`, `plan_tickers`, `latency` |
| Test | Test Matrix v1 | DONE | `9e35543` | 7 live scenarios (S1–S5b) on the post-C1 baseline, ~20 Groq calls. **5 PASS / 2 FAIL.** C1 proven from both sides: S4b (capacity trim → `capacity_drops`, gaps empty, Evaluate LLM ran) vs S5a (real NVDA-2021 corpus hole → `coverage_gaps`, drops empty, Evaluate skipped, 4.77s vs 23.41s). S3 FAIL — missed its own Expect line (`low/llm` vs `high/none`); initially mis-scored against its narrow fail-signal only. S5b FAIL — out-of-corpus ticker rail bypassed; recon-confirmed the Plan LLM never emitted the ticker, so `validate_tickers` had nothing to drop (gate not broken, gate never received input); footer key and single-write-site both ruled out. Full results, ranked v2 signal, matrix design errors and proposed v2 scenarios in docs/specs/S_test_matrix_v1.md |
| S_CR | Company Honesty Rail — Phase 3 (live verification) | VERIFIED, NOT FIXED | `c727fba` (no code change) | Verdict **D-B**: plan fields byte-identical across a 3-run fixed-input triple, answer text NOT. Structured-output path deterministic, free-text stream is not. Consequences: (1) **AC7a FAIL** — out-of-corpus answers use retrieval-attempt phrasing ("were retrieved") that wrongly implies a retry would help; 1/3 on fixed input. (2) **K1, larger than AC7a** — the low-confidence caveat (`prompts.py:141-142`) is a shipped honesty rail that fires on only 1/3 of low-confidence answers despite the flag being rendered unconditionally (`prompts.py:189`); 5-link chain verified by reading, not inference. (3) **Zero successful financial answers** — 4 `(company, metric)` cells requested across the pass, 4 missed, every run `confidence=low reason=llm`. This gates the v1 DoD (see v8 §13.4 amendment). Three rubrics frozen pre-fix so post-fix PASS stays distinguishable from rubric drift. Phase 4 (the fix) scope not yet locked. |
| S_CR P4 | Company Honesty Rail — Phase 4 (deterministic caveat) | DONE, NOT LIVE-VERIFIED | `31ea6b6` | Closes **K1**, the larger of the two Phase 3 defects: the low-confidence caveat is now emitted by CODE, never requested from the model. `synthesize_node` wraps the breaker stream in `_prepend_caveat` (nodes.py), which yields `LOW_CONFIDENCE_CAVEAT` first and then delegates. The wrapper sits OUTSIDE `ctx.breaker.stream(...)` so S14's lazy gate still fires at drain (G3) — pinned by an OPEN-breaker test whose LLM fake raises `AssertionError` if `astream` is ever touched. Prompt side removed entirely: bullet 2 of `SYNTHESIZE_SYSTEM_PROMPT` and the `Confidence flag:` line are deleted, and `build_synthesize_user_msg` no longer accepts `confidence` (2 call sites swept — the node and the test helper; verified zero remain). Caveat wording deliberately carries no retrieval-attempt language, so it does not reintroduce the AC7a surface. 4 new unit tests (low → caveat is first token; high → wrapper adds nothing, stream byte-identical; OPEN breaker + low → caveat then degraded preamble; `Confidence flag` absent from the built message); 236 passed / 7 skipped, `mypy --strict` clean, ruff clean, fakes only — zero Groq calls. **AC7a phrasing fix NOT in scope** — deferred to v2. |
| S_CR P4 | Company Honesty Rail — Phase 4 rubric (c) regression check | PASS (n=1), NOT CLOSED | `6a3810d` (docs only, no code change) | Measures whether **job (b)** of the deleted `SYNTHESIZE_SYSTEM_PROMPT` bullet 2 regressed. That bullet carried two jobs: (a) the low-confidence caveat, (b) disclosing when a specific figure is absent from the retrieved passages. Phase 4 replaced only (a), in code, via `_prepend_caveat`; **(b) has no replacement.** Run: single live invocation, `scripts/run_query.py` at HEAD (sha256 `d1d673e009c0d68a93b29243eb68193cf15d1d87cede76cae7b724fbcf98f822`). Query: `"What was Apple's operating margin in fiscal 2024?"` (sha256 `afce2c3c27f3ff20f1ffad83975534b6802753b54e6223784033b6530d492f15`). Footer: `confidence=low`, `reason=llm`, `coverage_gaps=[]`, `capacity_drops=[]`, `unavailable_tickers=[]`, `unavailable_years=[]`, `latency=8.77s`. Result: the answer explicitly disclosed that no operating-margin figure was present in the retrieved passages and declined to state a number; the deterministic Phase 4 caveat also fired as the first token on this previously untested query. **CAVEAT — this is NOT closure. n=1.** PASS is weak evidence: the disclosure appeared WITHOUT bullet 2, and n=1 cannot distinguish (i) residual prompt guidance, (ii) default model behaviour, (iii) lucky draw. What is established is only that the job-(b) regression is not catastrophic. **Do not record rubric (c) as "clean" or "closed".** Not pooled with run7/8/9 — different query, different estimand. Three side observations from the same run logged to v2/housekeeping below (retrieval skew, CJK citation brackets, footer key drift); none fixed here. |
| S_CR | Company Honesty Rail — AC1 live verification | PASS (n=1) | `37e4b41` (harness footer only, no agent code change) | Records the outcome of spec **AC1**, the headline S5b live case, which had no recorded outcome anywhere in this document until now. Recon of the repo for pre-existing AC1 evidence returned **NOT FOUND** — the Phase 3 row records the verdict, AC7a, K1 and the zero-financial-answers finding but never AC1, so **this run is the first recorded outcome**, not a re-confirmation. Commit under test: `37e4b41`. Query: `"Compare Coca-Cola's and Apple's operating margin in fiscal 2024."` (sha256 `feb173c2f8e75fc29ba9e7bc8b4fe9927746904c332b1f525a83870402136a40`), harness `scripts/run_query.py` (sha256 `a04a72fdab2f16c4b8852cedffd50c94493b757475b37edb552ed1ed72c26407`). Footer verbatim: `confidence=low reason=llm coverage_gaps=[] capacity_drops=[] unavailable_tickers=[] unavailable_years=[] unavailable_companies=['Coca-Cola'] plan_tickers=['AAPL'] latency=12.22s`. Verdict **PASS (n=1)** against the pre-registered rubric (`unavailable_companies == ["Coca-Cola"]` AND `plan_tickers == ["AAPL"]`) — both conjuncts hold: the out-of-roster company was carried verbatim as the user wrote it, and the in-roster company resolved to exactly one ticker with no invented cell. Scope: AC1 only — **AC2 and AC3 were not run and remain unrecorded.** Side note: **AC7a reproduced** — the answer used retrieval-attempt phrasing ("no filings … in the retrieved set"). Known, v2-deferred, not a new finding. |
| S_agent_pool_timeouts | Agent Pool Bounded Waits | DONE | `2b11b01` | **Root cause:** of the three waits on the agent pool only connection establishment was bounded (asyncpg default 60), while acquire and query were both unbounded — so a live run stalled ~5 min in silence, burned a Groq call, and logged nothing after Plan. **Load-bearing finding:** `pool.fetch(q, timeout=T)` bounds ONLY the query leg — `pool.py:613-634` calls a bare `self.acquire()` and never forwards `T`; `pool.py:891-899` shows `timeout is None` awaits `self._queue.get()` with no `wait_for`; and `Pool.__init__` has no timeout slot, so there is no pool-level acquire default to inherit and every acquire call site must pass its own (asyncpg 0.31.0, read from the installed source, not from docs). **D1** two-budget explicit `pool.acquire(timeout=A)` then `con.fetch(..., timeout=Q)`; a single `asyncio.timeout()` wrap was REJECTED because it collapses acquire-exhaustion and slow-query into one indistinguishable failure and those have opposite fixes (pool sizing vs SQL/index). **D2** `command_timeout` on `create_pool` is load-bearing, not defence in depth — `register_pgvector` runs as `init=` on every new connection and its codec round-trips can be bounded no other way (proven live: `AGENT_COMMAND_TIMEOUT_SECONDS=0.05` made the pool fail to BUILD, traceback inside `register_pgvector`); its floor is therefore cold-start cost, not query cost. **D3** connect timeout tightened 60 → 30. **D4** three config fields `agent_pool_connect_timeout_seconds=30.0` / `agent_pool_acquire_timeout_seconds=30.0` / `agent_command_timeout_seconds=10.0`, `_seconds` suffix per `breaker_reset_timeout_seconds`, env-overridable via `case_sensitive=False`. **D5** diagnosability in scope, wire contract NOT — no new exception type, no `ErrorEvent` change, no file under `src/alphalens/api/` touched (S18 D1 wrap-only preserved). **OQ1 closed — shape A (explicit kwargs):** `hybrid_search_cell` has neither `Settings` nor `AgentContext`, but `retrieve_node` one frame up has both and already threads four `cfg.retrieval_*` values down as kwargs; two floats reach the cell, not three (the connect timeout is a `create_pool` kwarg only). An `AgentContext` field was rejected — the function takes `pool: Pool` by design, so that shape pays for 6 construction sites plus the `context_schema=AgentContext` binding and still passes floats anyway. **Measurement basis (zero Groq):** densest cell JPM/2023 @ 1,934 embedded chunks, seeded random unit vector, live `jina_dimensions=768` (NOT 1024 — `_lock_jina_dimensions` enforces L17 and the column is `VECTOR(768)`); steady state ~0.21 s/cell, worst first query ~1.08 s, pool build 1.4–2.3 s, all against a WARM endpoint — a cold Neon autosuspend resume is not represented and is exactly what the 30 s connect budget absorbs. **G1 live gate PASS (zero Groq):** `AGENT_POOL_MAX_SIZE=1` + `AGENT_POOL_ACQUIRE_TIMEOUT_SECONDS=1` holding the only connection raised `TimeoutError` in 1.002 s with the log naming both the leg and the cell; the query leg was verified separately at 0.02 s. The `acquired` flag is what keeps a query breach from being logged as an acquire breach. `TimeoutError` needs no import and no `BaseException` change — on 3.12 `asyncio.TimeoutError is TimeoutError` and the MRO is TimeoutError → OSError → Exception, so S18's `except Exception` already maps it to an `ErrorEvent` ("TimeoutError during graph"). **`return_exceptions=False` deliberately unchanged** at both fan-outs (`nodes.py:473` embeddings, `nodes.py:484` cells): one breached cell fails the whole node rather than silently returning partial retrieval, which is the exact failure class the honesty rail exists to prevent. Second-order benefit: stragglers left running after the first raise are now capped and release via `async with`, so the pool drains on a bounded schedule instead of never; partial retrieval with explicit gap reporting is v2. Carried: `acquire_timeout` doubles as the release budget (`pool.py:931-932` falls back to it) — harmless at 30 s but relevant if anyone tunes it down; the cold-start site runs inside the FastAPI lifespan ABOVE the `try:` at `app.py:69`, so a breach there kills startup with no `ErrorEvent` path — accepted, loud beats silent. Commits `e619a92` (config), `0c5cbcd` (cold start), `2ce0057` (per-cell), `2b11b01` (tests); +9 tests, 294 passed / 7 skipped / 1 deselected; new `tests/unit/test_context.py` — `context.py` had zero coverage before; spec docs/specs/S_agent_pool_timeouts.md |
| S19 | Local Frontend (Next.js + SSE) | DONE | `7b8887e` | D1 npm (pnpm absent); scope `frontend/` only — zero files under `src/alphalens/` touched, S18's wire contract consumed as-is, never amended. Seven commits: `42e5e49` Next.js scaffold (C1), `1d3d69b` gitignore fix (C1's `frontend/.gitignore` `.env*` was swallowing `frontend/lib` and the env example), `bb8ff4e` wire types mirroring `schemas.py` (C2), `d79dc4f` SSE parser + `streamQuery` generator (C3), `a35d788` parser unit tests (C4, 8 passing), `95c27be` presentational components with the prop surface frozen at C5 not C6 (C5), `7b8887e` page wiring (C6). **Live-verified across 5 graph runs (budget was 3 — overrun recorded, not hidden), ~8–10 Groq calls:** AC8 both honesty lists populated on one run with distinct labels — `coverage_gaps` NVDA-2021 (NO EVIDENCE FOUND) vs `capacity_drops` AMZN-2024/TSLA-2024/V-2021 (TRIMMED TO FIT THE BUDGET), i.e. the C1 split visible in the UI; AC5 non-empty citations; AC14 `done` payload; AC2 (request body carries only `question`); G8 CORS (`Access-Control-Allow-Origin: http://localhost:3000` on preflight); `Content-Type: text/event-stream` + `Transfer-Encoding: chunked` + `Cache-Control: no-store`. **Ping-before-`meta` is structural, not an edge case** — pings fire on a fixed 15 s cadence from response start (not idle-only), so every run exceeding 15 s hits one before `meta`, which itself arrives only after `ainvoke()` returns (S18's known AC5 limitation); confirmed on three separate runs by arrival-time arithmetic; `sse.ts`'s `parsed === null → continue` guard survived it live having been exercised by no unit test. **AC3 (progressive token render) — NOT FALSIFIED, UNVERIFIABLE ON THIS CORPUS.** An earlier "AC3 PASS" is WITHDRAWN: it rested on a leading question ("progressively or in a lump?") answered by eye while the instrument sat open — assume-by-optimism, recorded as such. Measured 25 Aug 2026 from EventStream timestamps: `meta` and the deterministic caveat token (`_prepend_caveat`, K1) share t=0; the first synthesis token lands t+1.3 s (t+2.1 s on an earlier run) — that gap is Groq TTFT, not client latency, and is consistent across two runs; all 312 remaining `token` events plus `citations` plus `done` fall inside one 0.1 s bucket. **Token spread = 0.7 s for 312 events**, below the resolution at which progressive and batched rendering are distinguishable by any instrument available here. `page.tsx` contains no batching, and S18's own G3 (server-side progressive delivery) passed on a different run — the claim stands; only the client-side twin is unmeasurable. **AC9 — half exercised, remainder UNEXERCISABLE.** Abort during the pending window confirms the incomplete label and the no-error path (D7: `AbortError` is a user action, never an error box). The retention clause — a partial answer surviving abort while tokens are in flight — has passed through no test and no run; two attempts missed it. Run 5 supplies the structural reason rather than bad luck: **the Stop window measures 0.7 s**, under human reaction time once row-recognition is included. Not schedulable without a longer synthesis, i.e. not before the v2 iXBRL re-ingest. `reader.releaseLock()` not throwing on a cancelled stream remains an UNVERIFIED claim — never grepped out, never run. **Carried, unfixed:** the multi-`data:` join in `parseFrame` is permanent SSE identity, not spec correctness — `json.dumps` escapes newlines so multiple `data:` lines never form, and a rejoined payload would carry a literal newline `JSON.parse` rejects; the code stays, the CLAIM was wrong both here and in the S19 spec Amendment, which is owed a dated additive amendment. `parseFrame` returns `null` silently on a raw CRLF frame — contract-guarded, not code-guarded; unreachable in v1. **AC12's own instrument is weak:** measured, `git check-ignore -v` exits 0 meaning "matched some pattern", and only `-q` exit 0 means "is ignored" — previously recorded inverted; spec NOT amended. Gate C's second collision was misattributed to a `create-next-app`-generated `.env.local.example`; that file was never committed and was not on disk at C6 recon time — the fix was still required, only the stated cause was wrong. AC5's empty-citations branch: no test, no run, code-level only. G4 multi-byte: not observed on the wire (`json.dumps` renders everything `\uXXXX`); unit test only. API base URL silent fallback `?? 'http://127.0.0.1:8000'` (`page.tsx:15`) — v2 footgun: in a browser `127.0.0.1` is the USER's machine, so a deployed build fails confusingly rather than loudly; argued against in chat but never written into the CC prompt — author's omission, not CC's deviation. Latency prints as a raw float (`25.74756342099954s`) — cosmetic. Component tests deferred: `@testing-library/react` + `jsdom` (~40 transitive deps) push `package-lock.json` from ~319 KB toward the 500 KB pre-commit ceiling, forcing a `--maxkb` bump outside S19's scope. AC14's "one run" wording is reconciled HERE (actual: 5), not by amending the spec. **Corpus finding, not a wiring finding:** 8 citations returned and zero revenue figures — every company answered "does not state total revenue"; retrieval found Item 7 / Item 8, but the numbers live in tables that `sections.py:112-119` deletes before chunking. Fresh evidence for the top-ranked v2 iXBRL re-ingest, consistent with S_CR Phase 3's "zero successful financial answers". S19's wiring is correct. **Two DevTools traps recorded so they are not re-walked:** the EventStream tab does not render comment frames (Chrome parses only typed events; a ping has no `event:`), and the Response tab is empty for a reader-consumed stream — the silence of either is NOT evidence. spec docs/specs/S19_frontend_local.md |

## Phase 1 ETL Backfill

**Status: 180/180 = 100% processed** (verified 2026-07-31; was 151/151 on 2026-06-13). Zero pending/processing/failed.

- Last 3 (JPM iXBRL filings) closed in S20 via empty-start token bucket + token-aware batching (≤6K tok/req).
- Corpus is 100% `jina-v3` embeddings — homogeneous vector space.
- Known quality caveat: 3 JPM filings parsed iXBRL-as-HTML → blob chunks (419/428/517) → weak retrieval on these until v2 parser.

## Deferred to v2 (quality)

Ranked by strength of live evidence from the test matrix (see docs/specs/S_test_matrix_v1.md).

- **D — iXBRL XML parser** + corpus-wide re-ingest (fixes JPM blob chunks). **Now the top-ranked v2 item, with concrete evidence:** tabular/derived financials are not retrievable at all — operating margin failed 2/2 in the matrix, and an Apple 10-K chunk surfaced with the table header and row labels present but the numbers absent. Prose figures retrieve fine. This inverts the original "dead `lex_arm` = missed figures" premise; root cause is ETL/chunking, so lexical-arm recon drops in priority. **Root cause located in code (recon, 31 Jul 2026), no longer inferred:** `sections.py:112-113` replaces every `<table>` with `_TABLE_PLACEHOLDER`, and `_normalize` (`sections.py:119`) then replaces that placeholder with a space. Table content is therefore DELETED before chunking and never reaches the corpus — a deliberate v1 deferral, not a bug. Per-section `tables_stripped` counts are retained in metadata. Live confirmation: AAPL 10-K FY2024 (cleanly sectioned, no iXBRL blob) has 31 chunks in Item 8 Financial Statements and zero containing a comma-grouped number; the string `391,035` (FY2024 total net sales) is absent from the filing. Scope is corpus-wide, not the 3 JPM iXBRL filings — corpus-wide only 896 of 25,661 chunks (3.49%) contain a comma-grouped number. Caveat: that regex is a proxy, not a measurement — figures written as "$7.7 billion" and values under 1,000 carry no comma. **Lexical-arm recon is closed as answered: figures are ABSENT from the corpus, so no retrieval-side fix can reach them.** Fix requires re-ingest of the full corpus (~180 filings, Jina quota) via iXBRL as primary source with an HTML table parser as fallback. Open decision, deferred to v2 planning: do extracted numbers land in chunk text (linearized, RAG-retrievable) or in the existing deferred `financial_facts` table (structured lookup)?
- **`filing_type` as a retrieval axis** — recon-confirmed structural: `filing_type` is a display field only, never in the retrieval WHERE clause and never part of any cell key (cells are `(ticker, year, sub_question)`; coverage is `(ticker, year)`). Quarterly volume therefore outvotes annual filings on annual questions BY DESIGN. Hit 3 of 7 matrix scenarios. Correctness risk, not cosmetic. **Live instance (rubric (c) run):** 8 citations returned, 5 from Item 1A Risk Factors, zero from the 10-K's Item 8 — an annual question dominated by quarterly filings.
- **Fix B — asymmetric per-ticker sub-questions** (different metrics per ticker); also resolves the Cartesian false-low in `compute_coverage_gaps`.
- **Lever #2 — sub-question-scoped reranking.** MSFT skew did NOT reproduce in the matrix (MSFT took the top-2 slots in S4b). This is n=1: insufficient evidence, NOT counter-evidence. Re-validate across multiple runs before fixing or dropping.
- **Section detector gaps** — MSFT/META/AMZN chunks unstructured; separately, R&D/revenue queries returned chunks labelled "Item 1. Legal Proceedings" containing cloud-revenue content (suspected 10-Q Part I Item 1 vs Part II Item 1 collision). Hypothesis, recon pending.
- **Parent-document retrieval** (spec committed, not implemented)
- **403 fallback bug** — nomic fallback triggers on 402 only; Jina signals balance exhaustion with 403 `AUTHZ_INSUFFICIENT_BALANCE`. Fix trigger; do NOT auto-nomic the existing jina-v3 corpus.
- **ETL corpus gap (M2)** — `list_filings()` reads only EDGAR's `filings.recent` array, silently skipping `filings.files[]` overflow pages; 9 (ticker, year) pairs missing. Fix spec authored (docs/specs/S_fix_edgar_overflow.md); backfill deferred here. M1 (NVDA 2021, `date_from` floor) intentionally unchanged.
- **Year-rail twin** — the `years=[]` deterministic vacuous-coverage hole, symmetric to the S5b ticker-rail bypass. Documented in S16, no scenario. **Untested rail (blind spot the matrix missed):** the year rail was never exercised — no scenario produced a non-empty `unavailable_years`, and it shares S5b's exact soft-guide + hard-gate shape, so it is suspect by symmetry. **Moved here from "v1 remaining" (scope decision locked by Jamil, 07 Aug 2026):** `docs/specs/S_company_honesty_rail.md` already defers this in its own "Out of scope / deferred (v2)" section — "it is a separate concern and a separate spec" — so the v1-remaining entry contradicted the spec.
- **Citation marker format unstable** — one run emitted full-width CJK `【7】` where others emitted `[7]`, same prompt and config. The frontend will link markers to citations; an unstable format breaks the parser. Reproduced in the rubric (c) run — citation markers rendered with full-width CJK brackets (U+3010/U+3011). Flag for S19 frontend parsing.
- **Alias table** — semantic alias / punctuation / prefix over-match in company resolution. Carries the residual S5b failure class: `validate_companies` catches false positives only and is NOT a hard gate (spec Gotchas, `docs/specs/S_company_honesty_rail.md`) — S5b is reduced, not eliminated.
- **AC7a phrasing fix (from S_CR Phase 4 split)** — out-of-corpus answers use retrieval-attempt phrasing ("were retrieved") that wrongly implies a retry would help. Unlike K1 this cannot be fixed by emitting a fixed string: the phrasing is generated mid-answer by the model, so it needs either a prompt-level rewrite that Phase 3's determinism finding says will bind unreliably, or a structural change to how out-of-corpus cases are rendered. Scope not yet locked; the frozen Phase 3 rubric for AC7a still applies.

## v1 remaining

- **S_CR Phase 4 live re-verification** — the split was taken as proposed: deterministic caveat emission landed in v1 (see spec table), AC7a phrasing went to v2. What remains is measurement, not code: K1 has never been run against the frozen Phase 3 rubric. Owes one fixed-input triple (~3 Groq calls) to move K1 from "fixed in code, unit-tested" to "fixed, measured on live output" — the rubrics were frozen pre-fix precisely so this comparison stays honest.
- **Shipping track (v1 only):** S19 local frontend (Next.js + SSE consumption; renumbered from v8's "spec 17a") — S18 FastAPI+SSE local app landed 16 Aug 2026 (`d2109be`, see spec table). **Lambda Dockerfile (was spec 13) moved to v2 deployment backlog (12 Aug 2026)** — reverses the 31 Jul 2026 §13.4a amendment's rationale for keeping it in v1 ("cheap insurance, validates image size"). Reason: recon (12 Aug 2026) confirmed `src/alphalens/api/` contains only an empty `__init__.py` — no FastAPI/SSE app exists to containerize yet, so the insurance has nothing to validate against until S18 lands. Specs 14/16/17b remain deferred to end of v2 per the 31 Jul amendment. **S19 landed 25 Aug 2026 (`7b8887e`, see spec table)** — the v1 shipping track is closed; what the frontend proved live and what it could not measure are both recorded in that row.
- **Housekeeping:** OQ5 unescape, AC14 regression test (live test is a `pytest.skip` scaffold — structural assertions still unwritten), config default-model reconcile.
